<Page x:Class="Biblio_Directory2.Book"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Biblio_Directory2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Book" Height="450" Width="800" Loaded="Page_Loaded">
    <Grid>
        <DataGrid x:Name="BiblioList" FontSize="16" FontWeight="Bold" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path = Name}" IsReadOnly="False" Header="Name" Width="180"/>
                <DataGridTextColumn Binding="{Binding Path = Author}" IsReadOnly="False" Header="Author" Width="180"/>
                <DataGridTextColumn Binding="{Binding Path = Publishing}" IsReadOnly="False" Header="Publishing" Width="180"/>
                <DataGridTextColumn Binding="{Binding Path = PublishingDate}" IsReadOnly="False" Header="PublishingDate" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
